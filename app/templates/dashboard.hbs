<div class="container">
    <div clas="row">
        <div class="col-xs-10">
            <h1>Building {{model.bs.stats.building.name}}</h1>
            <h3>Managed by: {{model.bs.stats.building.manager.name}}</h3>
        </div>
        <div class="col-xs-2 text-right">
            <p>Apartments: {{model.bs.stats.apartmentCount}} </p>
            <p>Active Residents:{{model.bs.stats.residentCount}}</p>
        </div>

        <div clas="row">
            <div class="col-xs-12">
               <p class="big-text">Account Balance: <strong> {{model.bs.stats.balance}} </strong></p>
            </div>
        </div>
    </div>

    </div>
{{outlet}}
