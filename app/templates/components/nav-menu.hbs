<div class="container">
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
              <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
              </button>
            {{#link-to "index" class="navbar-brand"}}CondoPal{{/link-to}}
        </div>

        <div class="collapse navbar-collapse">
          {{#if authenticated}}
            <p class="navbar-text"><span class="text-capitalize"><strong>{{username}}</strong></span></p>
          {{/if}}
          <ul class="nav navbar-nav navbar-right">
            {{#if authenticated}}
               <li><a href="" {{action "logout"}}>Cerrar Sesi&oacute;n</a></li>
            {{else}} 
                <li>{{#link-to "login"}}Iniciar Sesi&oacute;n{{/link-to}}</li>
            {{/if}}
          </ul>
        </div>
      </div>
    </nav>
</div>
