<div class="container">
    <div class="jumbotron">
        <h2>Seleccione un Condominio</h2>
        <div class="btn-group-vertical btn-group-lg" role="group">
            {{#each model as |condo|}}
                {{#link-to "condo" condo.id class="btn btn-default"}}{{condo.name}}{{/link-to}}
            {{/each}}          
        </div>
    </div>
</div>
{{outlet}}
